using UnityEngine;

public class DummyObject : MonoBehaviour
{
	// Start is called before the first frame update
	void Start()
	{
		Debug.Log("DummyObject Started.");
	}

	// Update is called once per frame
	void Update()
	{
	}
}
